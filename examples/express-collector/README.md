# Express Collector Example

Runs the collector with SQLite for quick local testing.

```bash
pnpm --filter ./examples/express-collector dev
```

The ingest endpoint is `http://localhost:4000/ingest`.
